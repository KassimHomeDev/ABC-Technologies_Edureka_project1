pipeline {
    agent any
    environment {
        ANSIBLE_HOST_KEY_CHECKING = 'False'  // Optional: Disables SSH host key checking
    }
    stages {
        stage('Run ansible master playbook: Deploy Artifact to Kubernetes') {
            steps {
                    sh 'ansible-playbook -i ./hosts --check ansible-masster.yml'
            }
        }
    }
}
